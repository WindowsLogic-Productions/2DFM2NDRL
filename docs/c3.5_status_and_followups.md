# C3.5 Status — Round Events Shipped

**Tests:** 62/62 green. Companion plans:
`~/.claude/plans/replicated-crafting-blum.md` (C3.5–C11),
`docs/roadmaps/replay_hierarchy.md` (player-facing model),
`docs/c3.5_round_events_ida_handoff.md` (IDA recon used to drive the hook).

## What landed

| Piece | File | Status |
|---|---|---|
| `SessionEventType::ROUND_START` (=8) / `ROUND_END` (=9) | `spectator_node.h` | ✓ |
| `RoundStartPayload` (7 B) / `RoundEndPayload` (9 B) packed structs | `spectator_node.h` | ✓ |
| `SessionEvent::u` widened 4 → 9 bytes; `sizeof` = 10 | `spectator_node.h` | ✓ |
| Encoders `EncodeRoundStart` / `EncodeRoundEnd` | `spectator_node.cpp` | ✓ |
| Decoder branches for both new types | `spectator_node.cpp` | ✓ |
| `WirePayloadSize` + `IsValidEventTag` updated | `spectator_node.cpp` | ✓ |
| Two in-memory→wire dispatch sites updated (`FlushBatch` + backfill chunk) | `spectator_node.cpp` | ✓ |
| Apply-time spectator log lines (informational; sim drives banners) | `spectator_node.cpp` | ✓ |
| Hop-1 daisy-chain relay branches | `spectator_node.cpp` | ✓ |
| Host `Append*` APIs with internal frame-delta computation | `spectator_node.cpp` | ✓ |
| `vs_round_function` MinHook detour (pre/post substate edge detection) | `round_events.cpp` (new) | ✓ |
| Hook install in `InitializeHooks`; reset on `Netplay_StartBattle` | `hooks.cpp` + `netplay.cpp` | ✓ |
| Test mirror updated; round-trip tests for both event types | `test_spectator_protocol.cpp` | ✓ |

Detour is FM2K-only (`#if !defined(ENGINE_FM95)`). Every round transition on
the host now produces an event in `session_events`; spectators log them
informationally and propagate via the relay branch.

`.fm2krep` files written via `WriteCurrentBattleFile` already contain round
events — the data is on disk, just not indexed yet (no `round_offsets[]`).

## Open follow-ups (not C3.5)

### C7 — `round_offsets[8]` in `.fm2krep` header
Extend `WriteSessionFileImpl` to scan the emitted ROUND_START events and
populate `round_offsets[8]` in the header. Per the conversation, this rides
on a header bump from the current 32 B `SessionFileHeader` to a richer
~256 B `FM2KSessionFileHeader` (v2). New fields proposed:

- `started_at_unix` / `finished_at_unix`
- `game_id[32]`, `p1_nick[32]`, `p2_nick[32]`
- `p1_char_id`, `p2_char_id`, `p1_color`, `p2_color`
- `rounds_won_p1`, `rounds_won_p2`, `match_count`, `match_index`
- `session_id` (u64, generated host-side at `Netplay_OnPeerConnected`)
- `round_count` + `round_offsets[8]`
- `reserved[64]`

Backwards-compat: bump `version` field from 1 → 2; v1 readers haven't shipped
to users (v0.2.8 writes but doesn't read), so we have one window to break-
and-bump cleanly. Detect-and-upconvert any v1 files in the wild.

### MATCH_END payload extension
Make `.fm2krep` self-describing without hub cross-reference. Extend MATCH_END
encode/decode + `Netplay_EndBattle` to carry:
- `winner_idx` (0=P1, 1=P2, 2=draw)
- `rounds_won_p1`
- `rounds_won_p2`

Small-surface wire-format break — same window as C7's header bump.

### C8 — seek-with-fast-forward
`Replay_LoadSessionFile(path, seek_target)` where
`seek_target = {ROUND_START, idx}`. Two-pass load:

1. Scan `body[0..round_offsets[idx]]`, push only PIN_RNG / RESET_INPUT_STATE /
   SOUND_INIT / MATCH_START into `pb_queue`. Skips earlier rounds' INPUTs and
   ROUND_*. Rebuilds engine state at the round's start without sim work.
2. Stream `body[round_offsets[idx]..end]` normally.

Then engages the existing C5.5 fast-catchup drain (render off, audio off,
PGI+UG unbounded) until queue depth ≤ `LIVE_LAG_FRAMES`. Wall-clock budget
~50 ms — feels instant.

### C10 — Hub schema v2
Forward captured `RoundEndPayload`s with the 0xCC outcome message into
`matches.json` `rounds[]`. Add `session_id` + `match_index_in_session` +
`schema: 2` field on records that carry the new data; v1 readers ignore.

### C11 — Launcher Replay browser
ImGui tree UI: Session → Match → Round. Click handlers dispatch to
`Replay_LoadSessionFile` with the right path + seek target. Lazy index
scan at panel open, cached in `%APPDATA%/FM2K_Rollback/replays/index.json`.

### FM95 round emit
Separate handoff — different binary, different round-state machine entry
point. Mirror the FM2K `round_events.cpp` pattern once the FM95 IDA recon
identifies the equivalent of `vs_round_function`.

## Open question for C7
**`session_id` source.** Options discussed:
- Generated host-side at `Netplay_OnPeerConnected`, broadcast as a new
  `SESSION_ID` event at the head of `session_events`. Unambiguous; ~10 bytes
  on the wire per session.
- Derived deterministically from `(host_seed_at_handshake, started_at_unix,
  p1_nick, p2_nick)`. No extra wire event but requires both peers to pin
  inputs at the same moment.

Recommendation was generated-and-broadcast — wire cost is noise.
